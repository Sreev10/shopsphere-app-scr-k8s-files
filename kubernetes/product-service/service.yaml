apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: shopsphere
spec: 
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - port: 8081
      name: http
      targetPort: 8081
      protocol: TCP